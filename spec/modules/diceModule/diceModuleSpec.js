define(['src/modules/diceModule/diceModule'],
    function(diceModule){
        describe('diceModuleSpec', function(){
            it('dependency is loaded', function(){
                debugger;
                expect(diceModule).toBeDefined();
            });
        });
    }
);